# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=coffee_management

# Server
PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret

# OpenAI — bật phân tích LLM (tùy chọn; không có vẫn chạy với tóm tắt rule-based)
OPENAI_API_KEY=
# Ví dụ: gpt-4o-mini, gpt-4o
OPENAI_MODEL=gpt-4o-mini
